// Program for reversing the given string (User defined function)

#include <bits/stdc++.h>
using namespace std;
string getreverse(string s){
    int i=0;
    int l=s.size();
    while(s[i]!='\0'){
        2
}
int main(){
    string s;
    getline(cin,s);
    cout<<"Reverse of the given string is: "<<getreverse(s);
}